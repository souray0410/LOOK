# Serial complete-configuration delivery

Authorized 2026-09-17: first cataract / ResNet50 / deep / 3416,
interpolation factor16, PCA latent32, best-forward correction. Preserve the
unmodified parent host, complete-train PCA, GCV, data split and no-correction
candidate. The original ten-dimension experiment remains a different identity.

One task must finish train fitting, paired development prediction, exact artifact
reload/frozen-state checks, selection diagnostics, paired intervals and figures
before the next task starts. The finite first package follows with same-q sequential
controls at input, first feature, middle, last sites. No new seed is released here.
A negative first result does not cancel any listed control.

No new allocations are requested by these scripts. Each child uses the common
atomic claim and complete GPU profile before full-cohort execution. Runtime/source
and experiment configuration are immutable. Paused/failed managers are recorded,
never blindly duplicated; the ongoing operational monitor reconciles actual steps
before resumption with the same identity. These entrypoints do not claim complete
automatic cross-allocation recovery on their own.

A single-configuration dev report is exploratory after dev selection, not proof of
superiority, independent test performance or a complete matched search-policy study.
