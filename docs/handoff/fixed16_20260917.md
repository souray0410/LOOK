# Fixed16 weekly package

User authorized on2026-09-17: finish one configuration's full best-forward search
before expanding spatial ratios. The new task explicitly records spatial_factors=[16]
and has a distinct config fingerprint/output. Reuse accepted same-host train PCA
artifacts only; vector nodes keep factor1 because they have no spatial axes.
The unmodified parent factors[4,8,16] remain historical provenance, never rewritten.
The applied grid is execution_factors(spec,parent), used for validation and fitting.

Main trajectory remains best_forward on all candidates under the accepted upstream
bank, then strictly downstream refitting. Stop when the maximum remaining dev gain
is nonpositive. New tasks start from their own baseline, not decisions from a wider
search. Equal-factor sequential controls remain separate. Seed3416 package, replay,
diagnostics and reporting precede later seeds. Old healthy workers continue until
the replacement passes resource/lifecycle acceptance; no cancellation of allocation.

This reduces the spatial search dimension; it does not promise16-fold walltime
improvement or shrink the host's input. Direct-PCA and mean-pooling remain later
matched studies, not results already obtained. Report old terminal-operator results
separately from this full trajectory. Deployment and acceptance must be recorded live.
