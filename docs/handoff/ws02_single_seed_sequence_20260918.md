# ws02：逐问题、单配置、首种子交付

2026-09-18用户授权：ws02快速迭代，LOOK与Radon_Bridge各一张卡；每个有辨识度的问题先完整跑通一个配置/种子，随后扩展不同问题。Ibex优先大队列与正式论文证据。两平台同样执行数据隔离、停止/恢复、验收与归档规则；不缩短训练冒充收敛。单种子支持探索性现象，不能证明稳定优越或统计等效。

## 本轮有限顺序

1. 已接受ResNet50/deep/3416方法包只引用，不重训。
2. LOOK GPU0：一个ResNet18/deep/3416完整方法包，固定同数据/初始化类别/训练配方/x16/q32/两态正收益树；PCA自由均值与自由低秩残差顺序执行。目标是检查原现象是否只存在于较大ResNet50。模型宽度改变会自然改变各位置特征维度，这属于骨干变化，不称纯深度消融。
3. 不连续堆ResNet34等近似变体；DenseNet保留为不同架构候选，先不自动派发。接下来优先外部方法A与A+LOOK；MMTM的具体适配与梯度/初始化/训练匹配验收仍是前置，不能拿换骨干替代这个问题。
4. R&B GPU1：单独核验旧小队列输入和父模型/六臂适配，形成独立有限包；正式3D大队列训练不与此混同。资源预留不等于训练已启动。

## 已实现的执行边界

`look.studies.cohort_sequence` 是已有 `cohort_delivery` 的有限有序入口，不申请GPU、不启动常驻调度器；配置逐一执行、同GPU方法顺序完成。共享原设备锁，每run有pipeline锁，序列另有唯一入口锁。重复启动复用严格接受的完整结果；失败配置隔离不盲重试，按身份保留故障。后继配置须先通过实际GPU预检再训练；读取原run完整断点，不造新种子。

每60秒尝试生成脱敏累计入口，完成配置自动进入同一横表；新配置保留完整配置、树路径、其他指标和区间。发布失败不杀健康训练。GitHub同步仍由原ukb核验后执行，不是远端每60秒推送。首次无人值守定时发布须以实际回执验收，不能据提示词宣称已完成。

本轮无需用户重新点击开跑；工程问题由负责人闭环修复。改变标签、划分、主要科学比较或训练标准的未授权取舍仍需讨论。未过门槛的后继明确显示待验收，不无限占卡或重复旧实验。
