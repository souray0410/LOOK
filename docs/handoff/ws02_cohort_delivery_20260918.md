# ws02小队列完整交付：2026-09-18

用户授权：LOOK优先，沿用ws02旧数据集，重新运行当前Ibex方法设计，不依赖旧修正结果；两张卡可用于本项目。Ibex继续，Uncertainty由另一窗口负责，本轮不处理其代码或队列。

## 有限研究问题及排期

1. 重新训练一个共同ResNet50深层融合宿主；青光眼小队列1264 train / 296 dev，3416。公开ImageNet V1初始化，不引用历史医学宿主；是独立初始化路线，不能冒充Ibex医学父模型再组装。
2. 宿主按原host训练器：FP32、真实microbatch16、有效batch128、AdamW、100轮上限/至少8轮/15轮无改善，必须平台停止及重放；不为周日截断训练冒充收敛。两模态共享确定性左右翻转，原配对、标签和图像预处理不变，test不访问。
3. 冻结同一宿主及完整train PCA参考。两卡分别完成residual_rrr / pca_free_mean；均自由均值、x16空间插值、rank32、同train PCA-GCV参考λ、正收益树、两种缺失状态。原算法模块直接复用，数据入口和执行资源绑定单列。
4. 每臂重放后生成同基线、路径/拒绝、资源成本及指标；两臂齐全才配对比较，10000次参与者bootstrap。dev用于路径选择，区间不消除选择偏差。没有新best-forward/tree重复性能实验。
5. 后继优先回应导师9月13日问题：具体外部方法A与A+LOOK匹配定义和实现审计，先完成一个可辨识的对照；不拿三个骨干或融合位置冒充三种外部方法。宿主融合位置及倍数属于后续机制，不抢拟合方法包。

## 统一归档边界

同一标准适用于所有机器。模型训练与项目修正均有稳定配置/运行身份、固定源码和框架、数据清单校验、环境、初始化、恢复状态与验收。GitHub存脱敏协议、变化记录、汇总和模型索引；完整权重、优化器/RNG、参与者预测及缓存留在授权存储。项目宿主是项目产物，不伪装成MHD_Models通用父模型；模型总索引引用项目拥有的产物和类型。

当前实现 `look.studies.cohort_delivery` 为一个有限依赖链：profile→新宿主→PCA→两臂→匹配报告，复用现有原子文件/运行锁与MHD算子，不是常驻GPU申请器。跨平台不伪造SLURM_JOB_ID，也不启动旧ws02历史队列。

## 状态边界

数据出口验收记录存在，1264/296；两张RTX5000 Ada 32GiB现场空闲。新代码/环境正在验收；本文件不是GPU开跑或研究完成的证据。实际启动、恢复和产物证据随后追加，不覆盖初始准备状态。报告默认中文为主，图轴、术语和解释同时审查。

## 已核验的实际执行

- 科学源码b64ea908edb2a5ae70d7c57af3a41e5599876838、框架c0a27ab；独立环境安装及43项数据/原生宿主/拟合核心CPU测试通过。框架独立安装，公共依赖只读引用原已安装torch2.8.0+cu128环境并保存freeze；不是自包含可搬迁环境镜像。
- run `2026_09_18_09_35_40`，配置指纹 `aec06fceafde5ac3706e37d71572f15a20a76b9158de635ffb448f2621fe5c4c`。GPU预检完成全296dev、两次真实effective128更新及保存后恢复继续；显存峰值6928990208字节。此检查不证明数值跨硬件等价或拟合阶段峰值。
- 首次正式入口因归档软链接的目标目录缺失，在任何正式参数更新前退出；故障/回溯/修复记录保存在原run的incidents/dangling_model_binding。核验原pipeline锁空闲后创建正确目录，不修改科学源码或配置，原身份接续。
- 修复后实际22→71次参数更新、epoch3→8、完整验证history和best/last检查点更新，正式训练已核验。模型文件在统一models/runs下，项目run引用同一份权重，不复制两个独立模型身份。修正两臂尚未验收，不把进程存在叫完整方法结果。

[小队列当前累计入口](../reports/current/small_cohort/README.md)与原Ibex结果分开展示；以后按同一run身份增量更新。私有模型索引只记录逻辑资产引用、校验值、访问/可用/验收状态，不上传参与者预测或大权重。

### 同run的后续自动接续

共同宿主已按平台规则完成：stop_epoch=27、best_epoch=12、270次参数更新、selected重放通过，296 dev完整输入macro-F1=69.2142%、macro-AUROC=74.4522%。这是完整输入宿主基线，不是缺失矫正增益。9/9完整train PCA基已验收；两卡实际启动各自方法，两臂均完成首前缀79个train批次的完整/缺失特征统计并进入候选评价。后续仍需两种缺失与全部选中路径重放。

发布器另存允许上传的汇总和模型索引，每个已接受权重以逻辑asset_ref和SHA登记；不复制参与者记录。CPU发布器与GPU科学代码分开部署。自查发现报告误用`auroc`字段，实际统一字段为`macro_auroc_ovr`；已以合成双臂、双状态、6个配对比较回归测试修复。运行中的b64ea90 GPU快照不热改；独立report_candidate_v2在两臂完成、原pipeline锁释放后仅修复这一已知聚合报告问题，其他错误不自动略过。原错误及报告源码SHA单独留痕，不重跑或改变科学产物。

## 2026-09-18：小队列结果与累计发布链核验

同一新宿主的两种自由均值拟合、两种缺失已自动完成；报告字段错误的有限CPU修复实际成功，GPU科学源与产物未改。root独立核验8份预测SHA、296人顺序及F1/AUROC重算、共同宿主和报告文件SHA。累计6行去重结果及六项配对区间见[当前小队列报告](../reports/current/small_cohort/README.md)。两方法差值区间均跨零，不能宣布胜者；本配置完成不等于整个周包完成。

累计发布器增加证据截止、发布核验时间、去重与冲突拒绝。5项测试覆盖重复发布不改字节/mtime、身份不匹配、预测被篡改、重复行矛盾、报告与方法记录不一致；失败保留上版。真实两次发布一致，独立回执为当前报告目录publication_audit.json。远端自动汇总与报告修复已验证；GitHub更新由原ukb定时核验后发布，本轮实际同步验证不冒充首次无人值守定时发布验收。其他项目/平台现场状态未被本次ws02核验更新。
