# 先比较拟合方法，再扩展：2026-09-18交付修订

用户授权调整执行优先级，以周日可解释的完整匹配结果为近期目标。批准的其余研究不删除，健康旧任务按原身份继续；本修订不开放test，不解除整个首种子周包的重复种子门槛。

## 有限参考包

固定白内障、ResNet50深层融合宿主、3416、原train/dev参与者、双线性空间插值16倍、斜率秩上限32、冻结宿主和正收益树。缺OCT与缺CFP分别完整执行，不合并不同缺失状态的预测。

首包包含自由均值低秩残差（residual_rrr）与PCA约束斜率/自由均值（pca_free_mean）两臂，保持均值自由度一致，比较子空间限制的影响。PCA约束均值（shared_pca_ridge）和自由低秩斜率/约束均值（rrr_shared_intercept）保留已登记身份，属于后续扩展。四臂构成子空间×均值的解释框架；各自独立搜索后的最终差值是整个策略差值，不是孤立的均值因果效应。均值机制优先在相同前缀、斜率和正则强度下分析，再决定完整搜索扩展的必要性。

首包每节点沿用训练集PCA投影的GCV正则选择规则，使用该方法自己已接受上游修正后的配对统计；不借用旧PCA挑出的路径，不新增dev调参。此规则是统一参考规则，并非自由低秩回归的最优GCV推导。原PCA数值实现结果继续单列；只有数值、选择和预测等价验收后才可作为对应新臂复用，不能重命名为新结果。

四臂都使用完整训练配对拟合、相同候选节点和dev开关规则，保留空修正路径。不因自由均值结果不好而省略对照或重复种子。秩匹配不意味着容量匹配，记录实际参数与资源。

## 后继顺序

1. 首个自由均值配置完整拟合、两缺失重放、多指标、路径、成本、图表与报告。
2. 首包两臂匹配报告；报告到截止时未完成的单元，禁止拼接旧末端分数排名。约束均值全树不作为首包前置。
3. 四代表起点与必要固定前缀机制对照，然后宿主融合位置、表示方式/降采样、秩与能量设置。
4. 完整首种子有限周包验收后释放其余种子；更广疾病/架构研究分阶段延续。

该顺序控制新任务派发优先级，不构造无意义的科学依赖。资源允许时有限包独立臂可并行。后续规模扩展保留无修正和匹配普通线性基线，必要代表竞争方法继续验证，不能凭单宿主3416胜者宣布通用最佳。

## 缓存与验收

原PCA潜变量不足以重建自由低秩拟合所需的完整协方差，禁止代用。新实现按完整数据/宿主/表示/上游修正身份保存各位置充分统计；同前缀多位置共享前向，已完成统计不重新前向，改变前缀重新拟合。缓存加速不能改变浮点/选择结果而不记录验收。

完整维度的稠密矩阵需要单独资源准入；超过预算明确拒绝，不偷偷压成PCA、不改秩/空间或训练样本量。先通过合成等价与恢复测试，再验收实际MHD提取、资源峰值、完整dev与序列化重放，然后接入原Claims和dispatcher。代码通过、登记、部署、实际领取、完成分别记录。

当前发布边界：实现与集成正在验收；本文是明确执行修订，不是正式GPU启动回执。最新回执在本页下方追加，科研结果仍由累计报告提供。

## 实验必要性决定及边界

用户随后明确要求不再新增best-forward与树的性能比较。相同候选、前缀拟合、dev评分及严格正开关下，完整正收益树包含best-forward路径，故其所选dev分数不低于后者；这是包含关系，不是实证发现。四项合成实例检查路径/前缀/修正一致及分数不等式。现有两个已完成结果保留，新重复性能实验不再派发；成本和泛化不由包含关系推出，本周不另扩张这类研究。

首个必要拟合包进一步收拢为 **PCA自由均值与自由低秩残差** 两条独立正收益树，均值自由度保持一致，重点回答子空间限制的实际影响。四臂身份全部登记，两个约束均值全树保留后续，不作为此次最早交付前置。均值问题优先做固定前缀、固定斜率与λ的机制消融，再判断是否需要独立搜索扩展；不能从这两个独立树结果宣称已隔离均值效应。

旧PCA完整结果已经通过重放，并非不可复现。新旧实现等价审查未完成时，不能将旧结果冒充新统一实现中的匹配臂；满足复用条件则显式复用，未满足才登记新对照。此变更是看到现有dev之后的探索性设计修订。

## 工程接入证据（2026-09-18）

科学源码 `c2c2ddf` 的86项针对测试已在本地与Ibex独立快照通过。四臂依赖与不可变spec已核验登记，首包只引用上述两臂。Ibex `look_fitting_priority_20260918` 保存登记、测试及启动回执；自由低秩残差有限manager已启动，GPU预检、正式拟合及两臂接续仍须分别核验。本记录不将manager启动等同正式计算，也不宣称未来队列已完成切换。

## 2026-09-18 05:34 UTC：首臂正式拟合与共同队列接入

自由均值低秩残差通过真实GPU预检：512名train拟合、32名train探针评价，约1014秒，GPU保留峰值3.29GiB、进程RSS峰值4.85GiB；134份预检产物SHA与身份独立核验。该预检不代表全队列峰值或跨租期恢复验收。原两次bootstrap失败发生于claim之前，配置环境与启动脚本修复后已越过原故障并进入正式流程。

原run `2026_09_18_07_46_10_688082` 已在51919716.23正式执行，缺OCT根前缀的9个位置共享完整维度统计已观察到224批，缓存持续更新。此处“正式”指全量train/dev入口，尚无新方法完整性能结果；旧PCA结果保持原科研截止。

既有LOOK CPU dispatcher及唯一待批watcher已安全接到`owner_v7`；10个LOOK待批项采用新绑定，9个R&B和1个native项逐项保持不变，共用原锁、claims及申请账本。实际新CPU周期无错误，watcher更新waiting_grant；领取核验确认运行中的residual_rrr不可重复领取、pca_free_mean可候选，约束均值两臂不在首包。新best-forward/顺序起点研究按批准优先级暂不新增派发，健康旧worker未停止。未向GPU进程发信号、未取消allocation。首次安全交接因旧CPU为session leader被拒绝；核验该session只有其自身后，版本化加入无同session同伴保护，4项隔离条件测试通过再交接，没有放宽salloc子进程或GPU保护。

当前完成的是首臂真实启动与CPU后继准入；PCA自由均值实际GPU执行、两臂匹配报告、全队列资源峰值及跨租期恢复仍未验收。不可将“候选可领取”写成“两臂已跑通”。

远端证据：`OPS/look_fitting_priority_20260918/followthrough_20260918_0530.json`、`owner_v7/{activation_receipt,admission_verified}.json`。最早到期的另一LOOK旧任务已由原owner自动写入expiry pause并返回paused/owner_finished，未作为科研完成登记。
