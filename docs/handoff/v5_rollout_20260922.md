# V5 候选迁移交接（2026-09-22）

当前是候选源码，不是正式发布或生产迁移完成。历史科学结果与其原始框架版本保持不变。

当前框架依赖已固定到正式 V5 提交 `1287681c08846e11364c81653048435482e772a7`、tag `V5`，正式 wheel SHA256 为 `c022b4f4b0fa1f29458ad1bf9e0d04f6773e9454ab3bd8c07d416294483aab48`；MHD_Models 当前 direct-V5 路径固定为 `cc16e74a8cfc705d69b3d31efe2daeec9404471f`。此前 `c78cfbdc9ec01e4dc1dc8ee04d04b2e6a8446808` 框架及 `228b1254b1be5c1209831b93f7262962fe5222b7` companion 候选上的验收记录保持原始 identity，不改写为正式版验收。核心与规范资产加载路径采用 V5 当前格式，不提供 V4 回退；逐项研究工作流的选优状态与缓存合同仍需按正式 pin 回放。旧资产需独立转换、数值回放和新身份登记后才能进入正式运行。

完整 CPU 单元测试 486 通过；后续调度状态修复的 runtime 子集 99 通过。 CPU 通过不替代 GPU 完整流程、资产迁移或生产验收。

优先任务仍须通过周包、依赖及排他领取检查；原 Ibex 暂停循环事件尚未关闭。

## 安装运行依赖

在独立虚拟环境完成本项目及锁定框架安装后，从已授权、精确 checkout 的私有 MHD_Models 安装运行组件：

```bash
python scripts/install_operations.py --source /authorized/MHD_Models --check
python scripts/install_operations.py --source /authorized/MHD_Models
```

安装器核对 commit 与 Python 源码树摘要，不自动获取私有仓库、不使用旧目录回退；其成功仅表示安装一致，不表示数据/训练验收。公开项目的 CPU CI 不获取私有源码；正式调度、原生训练及 GPU 准入依赖上述组件。

## 后续验收

取消固定 10 GiB 显存预留，按设备实际占用、完整生命周期峰值和并发预算准入，host RAM 规则保留。剩余历史配置和逐项目单卡流程仍需核实。先在独立快照验证迁移，再在完整更新边界交接，保护健康 worker 和 allocation owner，不热改其绑定源码。Ibex 继续由现有系统维护 24 张运行与排队总预算及释放后的轮流补位。

未完成：实际依赖资产清单、旧资产一次性转换、完整恢复及跨平台更新对照、ws02 单张 32 GiB 全流程、生产切换和原事件关闭。正式 V5 已发布；本文件后续各项仍按逐消费者验收边界判断，不能由框架发布状态外推为项目迁移完成。

## 单卡有界流程验收

真实 train/dev 各 4 人的有界流程，随机初始化父模型；不是完整研究运行或真实父模型迁移验收。已在单张 RTX 5000 Ada 上通过；[完整范围与结果](../acceptance/v5_bounded_workflow_20260922.json)。LOOK 检查训练/精确恢复、PCA/修正拟合、重载、评价和报告；R&B 检查机制梯度、mask 及精确下一次更新。原失败环境缺少依赖的日志保留；补齐声明依赖后使用独立输出重跑，无阈值放宽。

## 真实宿主及完整断点迁移

ResNet18 observed-deep 的真实选优权重和完整 optimizer-boundary 断点，已独立转换为 V5。原 microbatch16、有效 batch128、296 人 development，以及各连续3次更新通过 FP32 1e-5/1e-6 对照；完整断点由当前 V5 loader 恢复优化器、scheduler、RNG 和 progress 后再验更新。参数和 buffer 数值/精度保留，原权重、研究身份与训练历史不变。详见[验收摘要](../acceptance/v5_actual_host_20260922.json)。

这是一项已停止研究的隔离诊断，未重新开启研究训练；不代表其他宿主、PCA/修正产物、Ibex 或全项目迁移完成。第一次参考运行虽数值通过，但 V4 非计算文件与原安装不完全一致，保留为 superseded；正式证据采用按原 source_pins 全部核验的参考快照。

资源复核还发现未完成项：cohort/EmbraceNet 当前配置仍接受旧 gpu_reserve_bytes，MHD_Models 的旧 packed_review 入口还包含固定10/20GiB阈值。不能据已更新的共同准则宣称所有入口均已去除；需在独立候选中统一迁移配置/实现并验收，原冻结运行保持不变。

当前 companion 已移除无现役调用的旧 packed_review（固定10/20GiB与独立控制账本）；冻结历史执行保持原源码。新工作沿用已有的 measured prepared_owner/controller 与共享Claims。原b924回放证据仍保留其原执行SHA。

当前候选已补齐cohort/EmbraceNet：非零旧GPU reserve配置直接拒绝并要求独立迁移，新准备的EmbraceNet使用零固定预留；实际allocator按实时占用与已验证workload cap配置。Host/disk约束保留。完整500项CPU检查与GitHub CI35750482110通过；真实GPU资源回验通过。这些候选修改尚未热切换任何生产worker。

[新的零固定预留资源回验](../acceptance/v5_resource_replay_20260922.json)已通过：新候选4d25400/companion ebd28b5，在另一张32GiB Ada上以这项宿主的3GiB实测工作量cap，完整296dev及两个转换断点的各3次更新均符合既定容差。3GiB不是统一预留或其他模型上限。原配置与研究流程仍需逐项迁移，完整拟合链路未据此接受。

当前宿主选优状态统一由host_checkpoint.save_selected/read_selected写入和校验V5标识、研究identity、epoch和Node IDs；observed/mechanism/EmbraceNet训练及project/cohort/mechanism/EmbraceNet消费均接入。未转换V4文件直接拒绝，完整optimizer-boundary文件不能充当选优权重。MHD_Models独立转换器已有相同V5标识，原转换文件无需人为改动Tensor。其他训练路径和PCA/修正格式仍需逐项闭环。

本轮完整CPU508项通过；[当前选优读取器的真实GPU回验](../acceptance/v5_strict_selected_20260922.json)再次覆盖296dev和选优/完整断点各3次更新，数值符合原阈值。新的训练/恢复/PCA/报告有界集成亦已跑通，完整生产闭环仍未完成。

[第一套真实PCA基迁移](../acceptance/v5_pca_20260922.json)通过：完整1264train/296dev、9处输入逐位相同，原PCA/joint算子源码SHA相同。保留全部基矩阵数值和dtype，仅重建依赖身份；当前GPU load_only逐项复用，无重新拟合。独立转换器不进入正常加载器。初次驱动误用spec字段名而退出，原attempt保留；v2按实际factor/rank完成。选中修正算子组与全搜索恢复树仍单独验收。

[两方法、两缺失方向的选中修正算子迁移](../acceptance/v5_selected_families_20260922.json)通过：保留所有拟合Tensor，只更新对已转换PCA的引用；当前V5宿主上逐项重载、296dev推理，4份预测文件SHA与原结果完全相同。没有重新拟合或选择模型。完整候选搜索树/中断恢复缓存没有据此迁移，也未生产切换。

候选资源入口进一步统一：共享包按设备实际占用和显式任务预算设置 allocator，不再隐式继承旧工作站上限。当前 Git 锁已更新，正在执行的远端验收继续保留启动时的旧精确快照；其结果不冒充新入口的部署验收。

### Deployed research source coverage, 2026-09-22

Merged the exact deployed research lineage ending at `77f94be3ecc3a7632bf34aad5fc288d542036ad6`: balanced dropout and improved modality dropout are actual consumers, not covered by checking main alone. Current selected and resume paths now use strict V5 readers/writers; old files require offline conversion. IMD used the unsupported `replace` aggregation; its single-input nodes now use `sum, memory=False`. Existing frozen-encoder, missing-state and exact next-update recovery tests pass. CPU full suite: 536 passed on PyTorch2.8. First run's four IMD failures are retained in the private rollout ledger; fixed without tolerance changes.

Balanced-dropout current preparation binds source receipt/config/artifact hashes instead of hardcoded historical V4 serialization hashes. Current resources use zero fixed reserve and the shared measured allocator. The original scientific mask policy, optimization, source epoch and selection rule are preserved. This is candidate code acceptance, not conversion or deployment of all eight real hosts. Real-host and downstream PCA/search/cache closure remains required before production switch.


2026-09-22追加：另外四个真实宿主（ResNet50、MMTM、fusion features/middle）的8份选优/完整断点已离线转换，并通过当前严格V5 reader的296人dev与各3次16/128连续更新。原优化器、scheduler、RNG、progress在last路径恢复；原权重与历史保持。balanced dropout、EmbraceNet、IMD及这些宿主的下游PCA/修正/搜索缓存、当前spec引用仍未完成。 详见../acceptance/v5_four_hosts_20260922.json；生产切换仍未完成。

2026-09-23追加：balanced dropout 和 EmbraceNet 两条真实缺失模态训练路径的原始 best/last 共4状态，已按各自原始 mask/sampler 规则完成 V4 源与当前 V5 的完整296人 dev、各3次16/128连续更新对照。四状态随后离线转换为严格 V5 选优及完整断点，转换后的当前 reader 重载与同样的缺失模态目标回放通过独立校验；优化器、scheduler、RNG、mask/sampler、参数与 buffer 逐项保持。证据见[原始回放](../acceptance/v5_missing_hosts_raw_20260923.json)及[转换后回放](../acceptance/v5_missing_hosts_postconversion_20260923.json)。这是已经结束的原 V4 研究的工程诊断，不改写训练来源。原 spec 的父权重路径、源码锁和下游身份尚未转成当前 V5 引用，因此这两条路径仍**不能按原生产配置直接派发**。IMD 和搜索/缓存链、Ibex A100 续训、正式发布及生产切换也仍未完成。

随后完成两条**隔离候选执行修订**：[独立验收](../acceptance/v5_missing_hosts_current_revision_20260923.json)。离线工具构造当前 V5 父模型 receipt、spec、源码/框架/companion 文件锁及新 checkpoint 身份；原 Tensor、优化器、随机流、科学配置与 train/dev 历史逐项不变。当前正常 `validate → make_graph → read_selected/load` 入口直接使用新引用，在单张32 GiB Ada 的两条互斥 GPU lane 上，best/last 共4状态再次通过完整296人 dev 与各3次16/128更新对照（FP32 `1e-5/1e-6`）。原始文件未改写，原训练仍标 V4；候选执行修订不是新独立 seed。它们尚未接入生产 dispatch，`dispatch_authorized=false`。IMD、下游 PCA/搜索缓存、Ibex A100 跨平台续训、正式 V5 发布及全项目切换仍未完成。

IMD（Improved Modality Dropout，改进模态丢弃）另按**完整模态加两个缺失方向的同时交叉熵**单独验收。原源码、准入、冻结记录和原 best/last [已核对](../acceptance/v5_imd_source_20260923.json)；实际 V4 与 V5 [目标回放](../acceptance/v5_imd_raw_20260923.json)、严格 V5 断点[转换后回放](../acceptance/v5_imd_postconversion_20260923.json)，以及当前 spec、receipt、身份和正常 `validate → make_graph → read_selected/load` [执行修订回放](../acceptance/v5_imd_current_revision_20260923.json)均经独立复核。每次覆盖296人 dev 的完整、缺 OCT、缺 CFP 三状态和各3次16/128更新；FP32 `rtol=1e-5, atol=1e-6`。原数值状态和科学字段保留；原研究仍标 V4，不因迁移得到新的独立种子或医学效果。单卡候选的6 GiB allocator cap依据该目标路径约4.94 GB已测峰值，只适用于这个有限回放，不是固定显存预留或全训练生命周期验收。IMD 原下游 LOOK 拟合/搜索和冻结链仍未迁移，候选 `dispatch_authorized=false`。首次离线 CPU 结构检查因保存了单卡 CUDA RNG、当时 CPU 进程看不到 GPU 而退出；转换产物未被当作验收，修正为结构检查并在持有 GPU 锁的单卡进程完成真实恢复后通过。

LOOK 文档提交 `d41132a` 的 CI 发现原 IMD 单测把目标损失以不同括号顺序逐位比较，FP32 相差一个 ULP；仅将测试期望改为注册目标的原表达式，模型/损失未修改。`2d3f996` 的 CI 已通过；新增 IMD V5 执行入口的 `24cf1d6` CI 另行跟踪。上述工程验收不代表下游缓存、Ibex A100、生产所有权交接或 V5 正式发布。

### 下游消费者清点与运行验收入口，2026-09-23

本轮将仓库内仍固定检查 V4、直接读取旧 `graph_state_dict`、并使用固定 10/14 GiB 项目上限的单卡及两卡运行验收入口改为直接 V5 合同。LOOK 父权重现在必须提供 schema 为 `look_v5_selected_reference_v1`、framework API 为 `V5`、并由 SHA 与外部 identity 绑定的 `selected.json`，再经 `read_selected` 严格校验 V5、Node IDs 和 `model` 状态；缺文件、旧 schema、V4 checkpoint 均直接失败。审查还发现原共享脚本的 Radon_Bridge 分支会从旧 `dependencies_relative.json` 读取未声明 V5 合同的父权重，因此 LOOK 仓库入口现只接受 `--project LOOK`；R&B 必须使用其自己仓库内独立验收的当前 V5 资产合同。显存限制改为调用方提供该工作负载和设备类已经接受的完整生命周期 `gpu_budget_bytes`，固定显存预留为零。源码提交为 `92c97b8577f49c49d04673dbe5571720d2eb6f5c`；结构检查及脚本编译通过，真实 GPU 入口尚未执行，不能据此声称部署验收。

[完整消费者清点](../acceptance/v5_downstream_inventory_20260923.json)区分了已接受的宿主、单个九节点 PCA bank、四个选中修正映射，以及仍未迁移的完整搜索/恢复缓存、其他宿主的 PCA/修正链和生产 owner/spec。现有 WebCodex generation5 继续是研究 cache/selection 的唯一写入方；本轮没有启动重复 writer、研究训练、test 访问或 Ibex 新申请。LOOK 全项目仍为部分迁移状态。

正式 V5 发布后，又在 ws02 的隔离候选区将同一宿主、九站点 PCA 与四条选中修正映射绑定到 Framework `1287681c08846e11364c81653048435482e772a7` 和 MHD_Models `cc16e74a8cfc705d69b3d31efe2daeec9404471f`。[验收记录](../acceptance/v5_formal_downstream_chain_20260923.json)确认全部复制文件 SHA 一致，当前严格宿主 reader、PCA reader 与 family reader 均可读取，四份 296 人 development 预测摘要仍与先前接受结果一致；没有重新拟合、重跑搜索、访问 test、使用 GPU 或切换生产 owner。主机清点仍有 8 个含下游状态的 run root，合计 65 个 PCA Tensor、20 个选中 bank，以及 2505 个候选预测缓存、864 个 family moments 和 2464 个 prefix artifact Tensor。后面三类搜索/恢复状态尚未迁移，不能由这一条代表链外推为 LOOK 全项目完成。

随后对其中一个实际 run root 的非选中搜索与恢复缓存做了[只读完整性和正式 V5 身份审计](../acceptance/v5_search_cache_audit_20260923.json)。历史缓存的 97 个 prefix 决策、173 个候选 artifact、274 条预测引用和 61 份完整 family moments 均通过内部身份、SHA 和恢复结构检查；这只证明历史状态未损坏。正式 V5 PCA 转换为九个站点引入了新的显式 source identity，basis fingerprint 随之改变，四条 arm/pattern 搜索合同及其 prefix、artifact、moment 身份均不再相等。当前读取门禁因此明确拒绝这些缓存，不提供 V4 回退，也不把历史完整性当作当前执行权限。该 root 的非选中缓存必须在 LOOK 自有 Ibex 轮转 allocation 和现有 owner 下由正式 V5 输入重算；已单独接受的 selected inference artifacts 不受此结论影响。

2026-09-23 的[抢占事件复核](../acceptance/priority_preemption_incident_20260923.json)确认：旧部署源码的优先探针确实绕过周包过滤；被误探测的 seed3417 已进入显式隔离，正常准入继续返回 `waiting_whole_weekly_delivery`。另一条合格任务保持原 owner 并从 step 13 推进到 step 124，恢复后至少写入五条新决策且 worker log 持续更新，所以下游恢复已有真实证据。候选源码把优先探针统一到正常准入、先排他领取再请求父模型暂停，并对 owner/generation/spec/job 复核；在真实部署 lineage 的隔离源码叠加上 6 项测试通过。健康旧 worker 仍绑定原不可变源码，未热改、未取消 allocation，因此事件状态是“已遏制且恢复推进，修复未部署”，尚不能关闭。关闭门槛是旧 owner 自然释放后按原 LOOK 轮转切换修复快照，并观察一次合格交接没有再触发延期任务暂停循环。

正式 V5 非选中缓存重算已登记为[Ibex 项目轮转包](../acceptance/v5_ibex_cache_recompute_rotation_20260923.json)。登记不领取 claim、不申请 GPU，`dispatch_authorized=false`；实际执行须等待当前 owner 释放后由 LOOK 自有 allocation 领取，写入新的隔离 root，逐项记录输入身份与 SHA，并验证中断恢复和当前严格 reader。历史 97 条决策树保留为历史结果，但全部不能作为正式 V5 执行缓存复用。

第二条完整下游库存 `2026_09_18_09_35_40` 已执行[正式 V5 严格门禁](../acceptance/v5_resnet50_downstream_gate_20260923.json)。其 best/last 转换文件通过当前 V5 状态结构检查；既有 296 人 development 回执所记录的 framework `core.py`、`utils.py` 与正式发布逐文件同 SHA，原始与转换后证据相同。只有 last 包含完整 optimizer、scheduler、RNG 和 progress，其连续三次更新才作为精确续训证据；best 的更新回放只作权重诊断，不冒充完整续训。九站点原 PCA manifest 和四个 selected bank 均未损坏，但该 bank 没有满足当前 importer 的 `look_node_feature_replay_v1` 精确站点回放回执；不能用最终 logits 相等代替中间站点相等。迁移在 PCA 前 fail closed，四个 selected bank 保持未重绑，四棵非选搜索缓存也被当前严格身份门拒绝。私有隔离回执 SHA256 为 `eecfa9d3437ebf55324b3ccf3cd47c749fe9dcf7631d141a9ce646b6da67f918`；本轮未使用 GPU、test、生产 owner 或 generation5 writer。下一步只能在 LOOK 自有 Ibex 轮转中生成该宿主完整 train/development 九站点回放，之后才可继续 PCA 和 selected correction 重绑。

该九站点门槛现已形成[Ibex 有限候选包](../acceptance/v5_resnet50_feature_replay_package_20260923.json)。入口按 batch 原子写入 train/development 的参与者与九站点 shape、dtype、数值 SHA；中断后只接受完全相同的 journal，任何漂移都 fail closed。V4 reference 与正式 V5 check 使用各自精确源码，受限数据、原 best、正式 V5 best 和 PCA bank 已复制到 Ibex 隔离区并逐项核对 SHA；固定显存预留为零，12 GiB 是依据同一 ResNet50 已测 7.68 GiB peak 加特征记录瞬态余量设置的该包上限。启动脚本要求已有 LOOK rotation claim，claim owner 与 `SLURM_JOB_ID` 必须一致。当前周包 policy 尚未列入该候选，因此状态保持 `staged_waiting_weekly_registration`、`dispatch_authorized=false`；本轮未申请 GPU 或改动现有 owner/claim。

随后对[周包登记资格](../acceptance/v5_resnet50_feature_replay_weekly_gate_20260923.json)进行了现场只读审计。当前 policy 的 21 个 required cases 尚无 accepted receipt，并保留未完成前置；已部署 dispatcher 的有限 feed 和执行分支也没有 feature-replay 类型。只向 policy 追加包名不能产生可执行任务，反而会伪造登记状态。审计时原轮转仍有 7 个 LOOK allocation 运行、2 个等待，未观察到健康 owner 释放。因此候选保持 `registration_blocked_existing_dispatcher_incompatible`、`dispatch_authorized=false`；没有登记、claim、GPU 申请或 worker 信号。下一安全动作是在独立版本化快照中补齐严格 replay 类型与 receipt verifier，挂在当前 whole-weekly 前置之后，并仅在旧 owner 自然释放时通过原 owner handover 激活。

上述缺口现已在[独立 dispatcher shadow 快照](../acceptance/v5_resnet50_feature_replay_dispatch_shadow_20260923.json)中补齐：新增受限 feed/type，只允许 reference→check，执行前再次核验 whole-weekly，claim 由原 Claims 独占领取并传给 worker，且该类型明确不参与 priority preemption；完成 verifier 要求两阶段身份一致和 `all_site_values_bitwise_equal=true`。候选被追加到 versioned delivery policy，而历史 policy 保持原 SHA。Ibex shadow admission 返回 `ready=0`、`waiting_whole_weekly_delivery`，直接调用执行入口也在子进程前失败。审计时仍有 7 个 LOOK allocation 运行、1 个等待，未见旧 owner 自然释放，所以状态为 `shadow_registered_waiting_owner_rotation_and_weekly`，没有 claim、GPU 申请或生产切换。

集成复核随后补强了子入口所有权验证：不能只信父 dispatcher 已 acquire。入口现在从 `LOOK_ROTATION_CLAIM` 读取真实 claim，逐项核对 run、spec SHA、精确 `look-workflow-$SLURM_JOB_ID` owner、job、step、generation 和 claimed/running 状态，并通过 Slurm 控制面确认 job 与 step 活跃；这些身份同时写入最终 acceptance。伪造 owner、job、spec、run、state、step 和失活 Slurm 身份均有失败测试，Ibex CPU shadow 也独立验证了精确身份通过而伪造 owner 被拒绝。现场状态不变，仍未激活或派发。

2026-09-23 对原 whole-weekly 21 条 required cases 重新做了当前状态核验：6 条已有严格接受文件并处于 completed，0 条失败，15 条搜索仍待完成（其中 1 条 paused、14 条尚无状态），21 份 spec SHA 均与历史 policy 一致，release receipt 仍不存在。用于暂缓 best-forward/greedy 的 residual-RRR 与 PCA-free-mean 两条 family-search 前置已经通过完整技术验收，因此 `owner_v7` 的模式延期已成为过期控制状态。候选提交增加了 fail-closed 的 priority release receipt：解除延期前核 receipt、spec、accepted receipt 和完整 family-search evidence；同一 owner 进程仅做一次完整证据遍历，后续周期仍核轻量身份，避免每 10 秒重复扫描大资产。Ibex 仅准备独立的 `owner_v9_weekly_release` successor 和 shadow，历史 owner/policy、allocation、claim、健康 worker、generation5 writer 与 test 均未改动。shadow 的精确数量、私有回执摘要与当前轮转状态见[公开脱敏证据](../acceptance/weekly_required_search_release_shadow_20260923.json)。它仍须等待现有 owner 自然释放并重新核合法 LOOK claim 才能激活；15 条搜索、五类周交付和版本化 release receipt 未完成，`independent_affine_best_forward_full_pipeline_not_yet_deployed` 不能清除。

2026-09-23 20:29 +03:00 对释放后自动接续又做了现场核验：[审计记录](../acceptance/owner_v9_automatic_handover_audit_20260923.json)确认当前持久 dispatcher 与 recovery plan 均固定并校验 `owner_v7`，不会读取 core priority-release receipt 或自动选择 `owner_v9`。`owner_v9_weekly_release` 仍是 shadow：没有 dispatcher binding、没有 standby 进程，其不可变 `dispatcher.json` 仍指向旧手工 receipt；当前另有 6 个 LOOK allocation 在运行，1 个已排队请求也绑定 v7。因此不能通过写 arm marker、改现有 binding 或改排队命令完成安全热切换。部署边界已经具体化：待 v7 的运行与排队 allocation 全部自然清空后，使用已接受 core receipt 及独立验证构建新的不可变 successor 包，在原唯一 dispatcher 入口执行 check、替换已停止的 v7 控制面、排他领取 LOOK claim，并以 allocation-owner/GPU-owner 回执验活；任一步不满足均保持 fail closed。此次没有领取 claim、申请 GPU、启动 dispatcher、修改健康 owner 或访问 test。

同日已在 Ibex 隔离命名空间实际生成 `owner_v10_core_successor_candidate`，将 core receipt 与独立验证摘要固定进新的 dispatcher、owner 和 binding；check-only 返回 0，未启动 standby/daemon、未领取 claim 或申请 GPU。对零 GPU `afterany` 接续也完成了可执行性复核，但没有提交：Slurm 依赖只能覆盖快照中的 7 个 allocation job，覆盖不了位于登录节点、仍可继续提交替代 allocation 的长驻 v7 CPU dispatcher，因此不能证明旧控制面自然释放，也不能保证唯一 writer。安全自动触发还缺一个由原 v7 控制面产生的持久 terminal receipt；该 receipt 出现且确认其后无新 v7 submission 后，才可让一次性 CPU job 重新核 Slurm/共享锁并启动已 check 的 successor。当前把 `afterany` 提前提交会制造竞争控制器，故保持失败关闭。

获得安全暂停旧任务授权后，又核到 v7 dispatcher 原生以共享输出目录的 `stop.json` 停止后续派发循环，并不会取消现有 allocation 或向 GPU step 发信号；但不可变实现退出时不写 terminal receipt，原 `status.json` 仍会停留在退出前状态。为补齐而不热改 v7，候选源码增加了版本化 drain receipt 工具：排他创建停止请求，等待同一 `dispatcher.lock` 可排他取得以证明旧 dispatcher 已退出，冻结最终 request journal，拒绝任何没有 Slurm job identity 的请求，再记录完整 terminal job 集合供 afterany 使用。相关控制面测试通过；由于原生 terminal receipt 缺失，本轮没有向在线输出写 `stop.json`，也没有提交 successor 作业。

在准备触发真实 drain 前执行正式 V5 身份门时，`owner_v10_core_successor_candidate` 被拒绝激活：其 check-only 确实通过且 core receipt 正确，但继承的 dispatcher 仍登记 `3559caa8`、`56065555`、`c0a27abb` 三个旧 framework commit，并保留 `legacy_project_pythonpath`；owner fallback 路径同样包含 `framework_c0a27ab`。因此该检查只证明控制结构可运行，不能证明登记任务已直接迁移到正式 Framework `1287681c` 与 companion `cc16e74a`。按照 fail-closed 顺序，没有写线上 `stop.json`、没有启动 monitor、没有提交 afterany。下一门槛是以正式 V5 资产清单重建 successor，并逐项证明所有可派发 feed 不会落回旧 framework/source；通过后才能执行已经准备好的 drain/terminal/afterany 链。

随后对 successor 当前登记的全部 feed 做了逐任务正式身份审计，结果见[隔离清单](../acceptance/formal_v5_successor_feed_audit_20260923.json)：176 条任务中 0 条可直接准入、176 条隔离等待。130 条 native spec 明确固定旧 framework（32/90/8 条分别为 `c0a27abb`/`3559caa8`/`56065555`）；其余 46 条下游 spec 没有同时固定正式 framework 与 companion identity，不能从文件名或宿主引用推断为 V5。正式 feature-replay 包尚受 whole-weekly 前置约束，也不在当前 active feed 中。候选源码增加了保守 admission 工具，要求每条 spec 显式携带 `1287681c` 与 `cc16e74a`，并拒绝 legacy execution path。由于没有任务通过身份门，本轮不伪造空 dispatcher check，也不拿隔离旧路径做所谓 V5 回放；在线 v7、GPU、claim 和 test 均未改动。

第一条竖切选择已接受 core arm `pca_free_mean` 并审计到资产级，结论见[竖切门](../acceptance/formal_v5_first_vertical_slice_20260923.json)。历史 run 保存了 correction bank、family moments、prefix decisions、development predictions 和完整文件清单；核心 operator 文件摘要未变，但 family-search 执行与统计代码已有显存准入和 resume 修复。阻塞不是“找不到旧权重”，而是正式上游身份缺失：旧 spec 固定旧宿主 run、旧 PCA manifest 和旧 source pins，当前正式 V5 证据只覆盖 296 人 development 下游链，没有 Ibex 上构成该拟合的完整 train-only frozen logits/features。直接复制 bank 并补双 pin 会把旧训练来源伪装成 V5。因此本条仍未解封，账本保持 0/176；下一合法路径是在 Ibex 物化正式宿主/PCA，重算完整 train cache 后重新拟合，并对 train moments、development predictions 与下一 prefix decision 做严格比较，失败则登记为全新 V5 重算而非等价迁移。

为把上述门落到真实可执行路径，Ibex 隔离区已生成当前 LOOK 源码绑定的 `pca_free_mean` 单任务包，并实际运行零 GPU preflight，见[执行门回执](../acceptance/pca_free_mean_v5_execution_gate_20260923.json)。包固定正式 Framework/MHD_Models 和既有九站点 feature-replay 包，覆盖 1264 train、296 development、V4 reference→V5 check，最终必须得到 `all_site_values_bitwise_equal=true`。现场仅剩 6 个健康 v7 allocation、无 pending；preflight 准确验证双 pin 与包/政策摘要后，因为 whole-weekly acceptance receipt 尚不存在而返回 75，未领取 claim、未申请 GPU。该包已具备同一不可变入口的自动重试条件，但当前不能绕过登记的周门；周门出现后仍须先取得 LOOK 原轮转排他 claim 才能执行。

后续候选提交补充了确定性的 priority-release 构建入口，并以输出命名空间锁消除检查与替换之间的并发窗口；相同身份重复执行不改写文件，不同身份、缺臂、spec 漂移或并发 writer 均失败关闭。Ibex 私有四臂登记现场复核显示，residual-RRR 与 PCA-free-mean 两个已批准核心臂具有完整接受资产，允许解除旧 best-forward/greedy 模式延期；二者生成的版本化 core receipt 已由 dispatcher 严格 reader 独立复核。该 receipt 只证明调度前置，不激活生产 owner。shared-PCA-ridge 与 shared-intercept RRR 尚无 run spec/accepted receipt，因此四臂完整科学比较、周包接受及重复种子门仍保持关闭。整个过程没有派发、领取或修改生产 owner。

### 2026-09-23 分站点全量 train cache 持久链

为消除旧单文件批次在逐站点 PCA 阶段约九倍重复读取，正式 V5 train cache 改用新的分站点身份；每批先写独立临时目录，九个站点文件及逐文件 SHA 完成后才原子提交。真实 A100 首块作业 `52388638` 完成 8/3651 批，写入 3,847,115,008 bytes，计算耗时 8.853 秒，`partial.json` SHA256 为 `373c188fa82e1c2995cc5731c9f091e84e66e0d1a7fef56a6fd270d4b5609bf3`。旧单文件 cache 保留且不混读。

首个零 GPU monitor `52388641` 因不可变 v1 脚本遗漏两个位置参数而以 2:0 失败，失败日志保留。新的不可变 v3 包同时修复 monitor 参数传递和 GPU 入口覆盖 claim 路径的问题；恢复 monitor `52388913` 已完成，并经共享 account lock、LOOK 原 request journal 与排他 claim 提交唯一 successor `52388915` 及 afterany monitor `52388916`。该链能按游标、完成目录和逐站点 SHA 严格恢复，容量不足仅排一个零 GPU retry，GPU 失败则关闭 claim 并停止。当前自动化边界只覆盖 3651 个 train materialization 批次；完整 train-only PCA、全 dev prediction 与首个决定尚未接线，不能据此称全量 replay 已完成。

### 2026-09-23 cache 完成后的 PCA 与首个 prefix 自动接续

候选源码增加了只接受完整分站点 cache 的 train-only PCA 阶段，以及只接受该 PCA receipt 的首个确定性 prefix 阶段。cache 门要求 3651 个连续批目录、完整游标、参与者顺序 SHA、正式 Framework/MHD_Models 双 pin，并在每个站点进入 moments/PCA 时核其文件 SHA；当前 partial cache 在创建输出前失败关闭。Ibex CPU 合同测试为 5 passed。

新的不可变 v8 控制包已接替仍处于依赖等待态的旧 monitor，不改正在运行的 GPU job：`52388915` 继续原 claim，唯一 replacement monitor 为 `52389487`。后续 materializer 在 Mac 离线时继续按共享锁与角色额度递归；只有生成完整 cache receipt 才提交 CPU PCA，只有 PCA accepted receipt 才在共享锁内领取 LOOK GPU claim 并提交首个 prefix 的 train/dev 运行。该 prefix 绑定九站点 PCA 顺序，但按已登记迁移合同只评价第一个站点；不得将其描述为完整九站点搜索或正式生产迁移完成。

### 2026-09-24 轮转身份硬门

对 v8 控制面审查后，当前 v10 使用既有且非符号链接的共享锁文件，禁止缺失时新建锁 inode，并在加锁前后核 descriptor/path inode；GPU 预算按 GRES 实际卡数统计；每个 live GPU job 必须恰好属于一个登记 journal；旧 claim 必须精确绑定 job/task/owner/cursor；PCA 只接受完整 cursor 与 cache identity SHA 一致的 `accepted_complete`。

真实 finalizer `52389857` 在保留 520/3651 批 cache 后失败关闭，未申请新 GPU。现场有四个 `ukb_cap128_01_48h` GPU job（`52292334`、`52288428`、`52362656`、`52362425`）未出现在 `research_gpu_roles.json` 登记的任何 request journal 中；不能依据 JobName 或命令路径猜测归属。须由其原控制面将每个 live job 绑定到唯一登记 journal 后，LOOK 才可用原 claim 重启同一 finalizer。该外部治理门不改变已经完成的 cache，也不构成 PCA 或 prefix 已执行。

### 2026-09-24 分站点 cache 的 PENDING allocation 一换一

完整复核 520/3651 批后，LOOK 七个角色位均被占用。旧 job `52422962` 明确为 `PENDING(Priority)` 且无 GPU step；在共享 submission lock 内取消该 job 和冗余零 GPU retry 后，登记 V5 successor `52429877`、排他 claim 和唯一 afterany `52430491`。没有停止健康 RUNNING worker。successor 当前仍因 Slurm Priority 等待，尚不能称为 A100 已运行或 cache 已超过 520 批。

未来 finalizer 固定为不可变 v20。每次执行使用控制面唯一 signer 生成 content-addressed v2 overlay，在共享锁内要求 overlay policy SHA 等于当前 policy SHA，并只允许已授权的 `afde7998...` 或 `efc45189...` 两个 policy 内容；projects 集合、LOOK reserved=7、native=0 与 account ceiling=24 同时保持不变。

2026-09-24 的后续只读审计确认，旧 LOOK dispatcher 会在启动时一次性读取共享 request journal，并在以后提交时把完整内存副本写回；因此 V5 successor 不能继续登记到该 journal。两阶段候选改为先从 `afde7998...` 只追加一个不可变 LOOK V5 专用 journal，生成 LOOK-only policy；Models 随后必须以该精确 policy SHA 为 previous，重新冻结只追加 Uncertainty_Lab journal 的 proposal。候选工具排他创建 journal、policy 和 receipt，并拒绝夹带 Models 变更。现有 v20 monitor 不接受任一新 SHA，且不得热改；在安装 LOOK-only policy 前，控制面须将仍为 PENDING 的 `52430491` 一对一替换为只允许 `{afde, look_sha}` 的不可变 monitor。Models policy 冻结后若 monitor 仍 pending，再一对一替换为同时允许精确 Models SHA 的新包。当前仅完成隔离实现与测试，没有修改生产 policy、Slurm 作业、共享旧 journal 或健康 owner；验收边界见 `docs/acceptance/look_v5_role_policy_two_stage_candidate_20260924.json`。

独立审查后，候选进一步要求 journal 行绑定 live Slurm user/account/ReqTRES/command/submit time、被替换的 `52422962`、已取消 retry `52429606`、replacement receipt 与 claim SHA。v21 只能从精确 v20 source SHA 派生，并将 v20 中后继提交所用的旧共享 journal 唯一替换为已登记的 V5 专用 journal。monitor 换代按 held-submit、核精确 afterany、取消旧 pending monitor、核旧终态、release 五阶段持久记录；崩溃恢复先按不可变 spec 查找唯一 held job，不能重复提交。若 Stage 1 monitor 已运行，Stage 2 必须等它终态，不能热换或安装其不接受的 Models policy。上述仍是候选实现，尚无 Ibex CPU shadow 或生产部署回执。

### 2026-09-24 V5 专用 journal 与 v21 monitor 正式交接

[正式工程回执](../acceptance/look_v5_monitor_policy_repair_accepted_20260924.json)记录了 held-first 生产切换：GPU job `52429877` 未中断、未重提，仍为 `PENDING(Priority)`；不接受新 policy 的旧 monitor `52430491` 已在无 step 时取消；唯一替代 monitor `52455266` 已释放为 `PENDING(Dependency)`，保持 `afterany:52429877`。共享 role policy 从 `b89feee...` 只追加一个绑定该 GPU job 的 LOOK V5 专用 journal，成为 `f07a65fc...`。生产阶段 `00` 至 `07` 八份回执连续，最终为 `release_verified`。

切换实现固定在 `5f5cc44596c8b3c98961d95a6f431a2ecd0ed913`；完整本机测试为 609 passed，真实 Ibex 零 GPU shadow 同时覆盖 policy-install acknowledgement 丢失、release acknowledgement 丢失和 `scontrol` 超时后的快速完成恢复。恢复必须同时重验原 held receipt、当前 Slurm command/user/account/ReqTRES、已清 dependency 和成功 `ExitCode=0:0`，没有通过只放宽状态谓词放行。

这次只接受控制面交接。GPU job 尚未获得 A100，也没有新增 cache 批次、optimizer update、PCA、prefix search 或科学结论；新 monitor 也尚未实际执行。只有 `52429877` 终态后 `52455266` 成功运行并产生后继回执，才能关闭自动接续风险。完整 3651 批 cache、PCA、搜索、test 和 LOOK 全项目 V5 迁移仍保持原门禁。

## 2026-09-25 早间实际接续

WS02完整宿主30 epochs/13253 updates已接受，完整九站点无损特征生成中。Ibex3651批/58403人全量特征实际完成，原后继误读旧partial游标而失败；已修复accepted优先读取及历史已结束GPU的overlay顺序，有限测试与真实现役overlay通过，CPU52559390正在核验剩余SHA后自动提交PCA及完整匹配链。未减少样本、站点或精度。

完成现役SHA核验→train-only PCA→完整匹配修正与dev报告；观察两平台各自互斥run，无需重提取Ibex已完成特征。

证据：`docs/acceptance/morning_execution_20260925.json`；hosted CI因额度耗尽未运行。
