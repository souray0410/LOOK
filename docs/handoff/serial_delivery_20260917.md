# 2026-09-17：首个完整配置交付与依赖缓存

用户批准的准则是完整配置交付优先，而非全局机械串行；不挤占交付链的独立任务
可以并行。PHD共同准则30c81e8。先首种子有限研究包贯通，不据正负结果删减对照。

部署源码[b6bbc27](https://github.com/souray0410/LOOK/commit/b6bbc2727255f4cd7c63c2108bb19a8073f137c2)，
[CI35188610391](https://github.com/souray0410/LOOK/actions/runs/35188610391)成功，Ibex18项针对测试通过。
这些不是正式科研完成。

入口OPS/look_serial_20260917_v2/sequence.json；manager PID1555558。
当前首任务DATA/LOOK/2026_09_10_11_11_31/search16_q32_20260917_v2/runs/2026_09_17_09_10_26_594899：
白内障/ResNet50/deep/3416、线性插值空间因子16、PCA潜在32维、best_forward。
2026-09-17 09:16 Saudi实际Slurm51909172.8执行资源预检，16个site cache已落盘，
日志继续更新。预检是512train/32train-role诊断，不能报成正式dev结果。

自动顺序：每条资源预检→全train拟合→配对dev预测→冻结/严格重放验收→
delivery单配置区间、诊断、图和中文释义→下一条。首条后是同16/32的四个
顺序起点1/2/5/9；五条完成后匹配总报告。未放行3417/3418。

复用原宿主、原完整train PCA与基SHA；已接受节点决策支持恢复。当前不同任务
的节点充分统计仍会重算，不能宣称所有特征缓存已共享；未来复用必须同时校验
数据、节点、基及上游已接受修正身份。上游变化使下游缺失特征改变。
旧fixed16十维grid及旧三因子任务保留原身份，新正式首site证据通过后仅请求这两条search暂停，
不触碰原主宿主/旧owner。已准备未运行的serial_v1被v2取代，无新增科学结果。

资源：原LOOK51919716重叠步骤请求存在CPU/RAM超额，禁止再叠加。新lane借已有
51909172，1CPU/6GiB；原3D父模型14CPU/100GiB+owner1CPU/2GiB，共16CPU108GiB，
满足128GiB主机15%余量。现场只读硬件检查卡余量通过；新lane运行中继续检查
全卡10GiB与自身RAM。旧source不热改。不能把1CPU限定推广为模型训练默认。

边界：这条当前原PCA/GCV搜索路径不代表八种独立仿射方法全部接通。
中断不改变run身份；sequence对已有launch拒绝盲重启，需维护者核对step/claim后恢复。
跨租期恢复的通用入口不能仅凭此顺序脚本宣称完整实现，三小时维护已更新负责接续。
