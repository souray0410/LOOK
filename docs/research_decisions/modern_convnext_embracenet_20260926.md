# Full-cohort ConvNeXt-Base EmbraceNet and LOOK

New registered design under the PHD modern-host authorization. Preserve the
completed small-cohort ResNet experiment and running matched reference suites.
No superiority is presumed. Main-window design, not attributed to the advisor.

Question: for one fixed missing-modality method A and selected modern host, does
train-fitted forward-tree LOOK improve missing-CFP or missing-OCT development
predictions? A uses the already audited EmbraceNet categorical coordinate selection,
Linear+ReLU docking and equal three-state training. This is not a reproduction
claim for the author's entire original data/training protocol.

Use the accepted full participant-level UKB cataract train/development split;
test remains sealed. CFP/OCT B-scan ConvNeXt-Base selected parents235970/469225
require explicit V5 full-development/three-update migration and current-artifact
consumer checks. Preserve original V4 training history. Parent development-based
selection makes this exploratory, not a held-out generalization result.

Two complete 2-D encoders, observed-eye mean pooling, embracement256, new binary
head. Preserve four native stages, downsampling, final normalization and all
parameters. ConvNeXt is not Swin NHWC. Nine correction sites retain the registered
input/stem/stages/features/participant/embraced semantics.

First finite seed3416 package: AdamW, FP32, microbatch16/effective128,
pretrained LR1e-4/new LR1e-3, weight decay1e-4, clip5, warmup5, minimum8 epochs,
patience15, cap100, unweighted CE. These are explicit choices from the audited
method comparison, not optimal-ConvNeXt claims. Resource failures cannot silently
change microbatch, precision, data or scientific identity.

Select A by complete-development macro-F1 with exact integrated mean logits;
freeze the same A for both comparisons. Train-only fit, two missing directions,
PCA free-mean and residual RRR, rank32/factor16, positive-forward tree. Preserve
all candidates and empty trees. Report simultaneous paired intervals and full
fit/search cost. Missing-state metrics cannot replace host selection.

Required chain: converted parents -> actual A100 forward/backward and fresh-process
recovery -> full-data resource qualification -> host training/selection -> analytic
stochastic-second-moment PCA -> fitting/tree search -> matched full-dev report.
Bind source, data, parent identities and predecessor receipts at each stage;
lease-safe continuation is required. A CPU model test does not accept this chain.

Implementation09-26: native ConvNeXt-Base cuts and EmbraceNet graph implemented;
25 CPU tests passed, including native feature equality, branch gradients and
sampling replay. Full package entry, GPU recovery and method execution remain
open. Do not relabel an old small-cohort spec to bypass its finite contract.

Implementation follow-up09-26: source69505dc adds the separate full-cohort method
entry, native observed-eye dataset adapter, mandatory lease end, and an independent
process for update1-to2 recovery before host training. Seventeen targeted CPU
checks pass. Selected-parent publication d05fd8d is deployed as CPU successor to
the real A100 migration qualification, with strict current-artifact consumer
checks. The full modern scientific GPU package and automatic lease reapplication
are not deployed or accepted yet; do not infer those from this implementation.
